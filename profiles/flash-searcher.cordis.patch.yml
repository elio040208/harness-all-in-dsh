- insert:
    - id: harness-all-in-dsh
      name: harness-all-in-dsh
      config:
        harness: flash-searcher
        summaryInterval: 8
        protocolMode: strict

- id: agent-loop
  config:
    maxParallelToolCalls: 5
