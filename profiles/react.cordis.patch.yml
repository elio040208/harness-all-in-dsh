# Apply after installing this bundle into a profile cloned from `headless`.
- insert:
    - id: harness-all-in-dsh
      name: harness-all-in-dsh
      config:
        harness: react

- id: agent-loop
  config:
    maxParallelToolCalls: 1
