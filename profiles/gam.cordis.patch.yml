- insert:
    - id: harness-all-in-dsh
      name: harness-all-in-dsh
      config:
        harness: gam
        summaryInterval: 8
        reorgInterval: 4
        protocolMode: strict

- id: agent-loop
  config:
    maxParallelToolCalls: 5
